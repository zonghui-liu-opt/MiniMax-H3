# H3 提示词约定

- 按用户 2026-09-08 的要求，后续为本项目设计或改写 MiniMax-H3 视频提示词时，始终使用官方 `h3-prompt-writing` skill，并阅读对应输入模式的参考指南。
- 以用户当前动作目标和实际传入的图片／视频条件确定模式、动作路径和结尾状态；官方格式不等于要求全程静止或首尾同图。
- 保留各版提示词以便对照；没有实际推理结果时，将新版标记为待验证，不宣称生成问题已经解决。

# 工程维护约定

- 保持代码库精简，不生成或保留工程压缩包、上游源码快照、重复验证报告。
- 提示词按版本单独保存，metadata 引用提示词文件；派生媒体、请求预览、日志和推理结果作为运行产物忽略，不提交 Git。
- dry-run 默认只检查；调试请求预览必须显式开启。保留完整的预检、批量推理和断点恢复功能，避免精简后需要额外搬运文件才能运行。
- Ref2VA 的运行与环境说明集中维护在 exp_Ref2VA/README.md；代码通过 zonghui-liu-opt/MiniMax-H3 的 data_pipeline 分支同步。
